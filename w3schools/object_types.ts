// this is how we add types to objects in typescript

const car: { type: string, model: string, year: number, mileage?: number } = {
    type: "Toyota",
    model: "Corolla",
    year: 2009
    // mileage is optional
};
