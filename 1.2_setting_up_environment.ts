// Node.js and a typescript compiler is required to work with typescript
// typescript compiler is a node module that compiles ts into javascript

// download and install node.js if not already
// verify node installtion with: node -v

// then run: npm install -g typescript
// (-g will install the module globally)
// test the compiler version: tsc --v

// to run TypeScript code directly on Node.js without precompiling, you can use the tsx module.
// > npm install -g tsx


// vscode and Live Server is recommended